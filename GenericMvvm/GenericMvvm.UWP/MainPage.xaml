<Page
    x:Class="GenericMvvm.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GenericMvvm.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" x:Name="gridHeader">
            <Grid>
                <StackPanel Padding="10">
                    <TextBlock>タイトル</TextBlock>
                </StackPanel>
                <ProgressBar x:Name="progressBar" VerticalAlignment="Bottom" IsIndeterminate="True"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1" x:Name="gridContent">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">起動中</TextBlock>
        </Grid>

        <Grid Grid.Row="2" x:Name="gridFooter">
            <StackPanel Padding="10">
                <TextBlock>フッター</TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
